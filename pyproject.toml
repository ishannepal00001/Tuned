[project]
name = "tuned"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yt-dlp",
]
[project.scripts]
tuned = "tuned.main:main"
